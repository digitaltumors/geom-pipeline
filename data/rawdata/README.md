# rawdata

Downloaded GEOM source files live here. The pipeline assumes source metadata under `rawdata/metadata/`, downloads `rdkit_folder.tar.gz`, extracts `rdkit_folder/`, and optionally downloads `molecule_net.tar.gz`.

Full GEOM archives plus extraction can exceed 100 GB. Subset test runs still need the source archive unless files are already present. Files here are ignored by git.
