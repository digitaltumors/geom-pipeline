# results

Final outputs live here:

- `geom_MultiAssayExperiment.rds`
- `geom_tables/`
- `geom_tables.tar.gz`

Expected usage is usually under several GB for the default drug-only output, depending on descriptor table size. Generated results are ignored by git.
